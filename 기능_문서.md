# Snuggle 기능 문서

> **작성자**: 성민혁 & 김권희  
> **작성일**: 2024.12.16

---

## 목차
1. [블로그 방문자 구현](#1-블로그-방문자-구현)
2. [블로그 내 검색 구현](#2-블로그-내-검색-구현)
3. [메인 페이지: 오늘의 인기글](#3-메인-페이지-오늘의-인기글)
4. [계정 관리](#4-계정-관리)

---

## 1. 블로그 방문자 구현

### 📁 관련 파일
| 위치 | 파일 | 설명 |
|------|------|------|
| Backend | `src/utils/visitor.ts` | 방문자 추적 로직 |
| Backend | `src/middleware/visitor.ts` | 방문자 미들웨어 |
| Backend | `src/routes/blogs.ts` | 방문자 수 조회 API |
| Backend | `src/scheduler/visitorSync.ts` | Redis → DB 동기화 |
| Frontend | `lib/api/blogs.ts` | `getVisitorCount()` |
| Frontend | `components/blog/BlogProfileSidebar.tsx` | 방문자 수 표시 |

### 🔄 동작 흐름

```
┌──────────────────────────────────────────────────────────────┐
│  1. 방문자가 블로그 페이지/게시글 접속                         │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  2. visitor 미들웨어 실행                                     │
│     - 쿠키에서 visitor_id 추출 (없으면 IP 사용)                │
│     - trackVisitor(blogId, visitorId) 호출                   │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  3. Redis 중복 체크 및 카운팅                                 │
│     - 키: visit:log:{blogId}:{visitorId}:{날짜}              │
│     - 오늘 이미 방문했으면 무시                                │
│     - 첫 방문이면 pending 카운터 증가                         │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  4. 스케줄러 (10분마다)                                       │
│     - Redis pending 값 → DB visitor_count에 저장             │
│     - Redis 키 삭제                                          │
└──────────────────────────────────────────────────────────────┘
```

### 📖 코드 읽는 순서
1. `src/utils/visitor.ts` - `trackVisitor()` 함수
2. `src/routes/blogs.ts` - `GET /:id/visitors` 엔드포인트
3. `src/scheduler/visitorSync.ts` - 동기화 스케줄러

---

## 2. 블로그 내 검색 구현

### 📁 관련 파일
| 위치 | 파일 | 설명 |
|------|------|------|
| Backend | `src/routes/search.ts` | 검색 API (4개 엔드포인트) |
| Frontend | `lib/api/search.ts` | API 클라이언트 |
| Frontend | `app/search/page.tsx` | 검색 결과 페이지 |
| Frontend | `components/search/SearchHeader.tsx` | 검색 헤더 |
| Frontend | `components/search/SearchInputWithSuggestions.tsx` | 자동완성 |
| Frontend | `components/search/SearchTabs.tsx` | 글/블로그 탭 |
| Frontend | `components/search/SearchPostCard.tsx` | 게시글 카드 |
| Frontend | `components/search/SearchBlogCard.tsx` | 블로그 카드 |

### 🔄 동작 흐름

```
┌──────────────────────────────────────────────────────────────┐
│  1. SearchHeader에서 검색어 입력                              │
│     - 2글자 이상: /api/search/suggest 호출 (자동완성)          │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  2. Enter 또는 검색 버튼 → /search?q=검색어 이동              │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  3. page.tsx에서 병렬 API 호출                                │
│     - getSearchCounts(query) → 탭에 개수 표시                 │
│     - searchPosts() 또는 searchBlogs()                       │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  4. Backend 처리                                             │
│     - ILIKE 검색: title/content (글), name/description (블로그)│
│     - 프로필/좋아요/댓글 병렬 조회 (N+1 문제 해결)              │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  5. SearchPostCard / SearchBlogCard로 결과 렌더링            │
└──────────────────────────────────────────────────────────────┘
```

### 🔍 API 엔드포인트
| 엔드포인트 | 설명 |
|-----------|------|
| `GET /api/search/count?q=` | 글/블로그 검색 결과 개수 |
| `GET /api/search/posts?q=` | 게시글 검색 (정렬, 페이징) |
| `GET /api/search/blogs?q=` | 블로그 검색 |
| `GET /api/search/suggest?q=` | 자동완성 추천 |

### 📖 코드 읽는 순서
1. `lib/api/search.ts` - 타입 정의 확인
2. `app/search/page.tsx` - 전체 흐름 파악
3. `backend/src/routes/search.ts` - API 로직

---

## 3. 메인 페이지: 오늘의 인기글

### 📁 관련 파일
| 위치 | 파일 | 설명 |
|------|------|------|
| Backend | `src/routes/posts.ts` | `GET /popular` 엔드포인트 |
| Frontend | `lib/api/posts.ts` | `getPopularPosts()` |
| Frontend | `components/home/PopularPostSlider.tsx` | 슬라이더 UI |

### 🔄 동작 흐름

```
┌──────────────────────────────────────────────────────────────┐
│  1. 메인 페이지 로드                                          │
│     - PopularPostSlider 마운트                               │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  2. getPopularPosts() 호출                                   │
│     - GET /api/posts/popular                                 │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  3. Backend: 인기글 기준으로 정렬하여 반환                      │
│     - view_count, like_count 등 기준                         │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  4. Embla Carousel로 슬라이더 렌더링                          │
│     - 15초 자동 재생                                         │
│     - 화살표/인디케이터 클릭으로 이동                          │
└──────────────────────────────────────────────────────────────┘
```

### 🎨 UI 특징
- Embla Carousel + Autoplay 사용
- 15초 간격 자동 슬라이드
- 프로그레스 인디케이터 (현재 슬라이드 표시)
- 썸네일 없을 시 그라데이션 + 노이즈 텍스처 폴백

### 📖 코드 읽는 순서
1. `lib/api/posts.ts` - `getPopularPosts()`
2. `components/home/PopularPostSlider.tsx` - 전체 컴포넌트

---

## 4. 계정 관리

### 📁 관련 파일
| 위치 | 파일 | 설명 |
|------|------|------|
| Backend | `src/routes/blogs.ts` | 블로그 CRUD API |
| Frontend | `lib/api/blogs.ts` | API 클라이언트 |
| Frontend | `app/account/page.tsx` | 계정 관리 페이지 |

### 🔄 동작 흐름

```
┌──────────────────────────────────────────────────────────────┐
│  1. /account 페이지 접속                                      │
│     - 로그인 체크 (미로그인 시 홈으로 리다이렉트)               │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  2. 병렬로 API 호출                                           │
│     - getMyBlogs() → 활성 블로그 목록                         │
│     - getDeletedBlogs() → 휴지통 (삭제된 블로그)               │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  3. UI 렌더링                                                 │
│     - 내 블로그 섹션 (생성/설정/삭제 버튼)                     │
│     - 휴지통 섹션 (복구 버튼)                                  │
└──────────────────────────────────────────────────────────────┘
```

### 🛠️ 지원 기능
| 기능 | API | 설명 |
|------|-----|------|
| 블로그 목록 | `GET /api/blogs/my` | 활성 블로그 조회 |
| 블로그 생성 | `POST /api/blogs` | 새 블로그 만들기 |
| 블로그 삭제 | `DELETE /api/blogs/:id` | Soft Delete (휴지통으로) |
| 휴지통 목록 | `GET /api/blogs/deleted` | 삭제된 블로그 조회 |
| 블로그 복구 | `PATCH /api/blogs/:id/restore` | 휴지통에서 복구 |

### 📖 코드 읽는 순서
1. `lib/api/blogs.ts` - API 함수들 확인
2. `app/account/page.tsx` - 페이지 로직

---

## 💡 빠른 참조

### Frontend 진입점
| 기능 | 파일 |
|------|------|
| 방문자 | `components/blog/BlogProfileSidebar.tsx` |
| 검색 | `app/search/page.tsx` |
| 인기글 | `components/home/PopularPostSlider.tsx` |
| 계정관리 | `app/account/page.tsx` |

### Backend 진입점
| 기능 | 파일 |
|------|------|
| 방문자 | `routes/blogs.ts` (/:id/visitors) |
| 검색 | `routes/search.ts` |
| 인기글 | `routes/posts.ts` (/popular) |
| 계정관리 | `routes/blogs.ts` |
